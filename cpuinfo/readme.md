# Compile

<br>
If not using Scons<br>

gcc -o cpuinfo cpuinfo.cpp `pkg-config --cflags --libs`<br>
The Quotes are not showing. So there's a single quote in front of pkg-config and one at the end of the line.
